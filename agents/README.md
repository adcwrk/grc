# Agent Roles

These role definitions are reusable operating profiles for AI agents. They supplement, but do not replace, `AGENTS.md`.

Each role must treat repository artifacts as authoritative and cite paths for GRC conclusions.
