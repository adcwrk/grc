# Claude Code Adapter

Follow `AGENTS.md`. It is the canonical repository-wide instruction file.

Before changing files, read `AGENTS.md`, `README.md`, `docs/architecture.md`, and the relevant client handoff/context files.

When the user requests client onboarding, read `agents/client-onboarding.md` and use `scripts/create-client` plus `scripts/validate-workspace`.
