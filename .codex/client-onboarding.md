# Codex Client Onboarding Adapter

When the user requests client onboarding, read and follow `agents/client-onboarding.md`.

Use `scripts/create-client` and `scripts/validate-workspace` for deterministic onboarding operations.
