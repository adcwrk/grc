# Gemini CLI Adapter

Follow `AGENTS.md`. It is the canonical repository-wide instruction file.

When the user requests client onboarding:

1. Read `agents/client-onboarding.md`.
2. Follow that specification as authoritative.
3. Use repository scripts for deterministic operations.
4. Persist engagement state to repository files.
5. Do not rely on conversation history as the system of record.
