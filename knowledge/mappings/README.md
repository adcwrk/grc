# Cross-Framework Mappings

Use this directory for mappings such as:

```text
CMMC <-> NIST SP 800-171 <-> NIST CSF <-> ISO 27001
```

Mappings should cite source artifacts and avoid copying proprietary text.
