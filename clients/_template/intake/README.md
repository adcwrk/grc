# Intake

Use this directory only for source material approved for storage in Git. Otherwise, keep source files in an approved external location and catalog them with `scripts/create-client --source PATH`.
