# Claude Code Example

Prompt:

```text
Onboard Acme Corporation using the canonical client onboarding workflow.

The engagement is a CMMC Level 2 readiness assessment and the source material is in ./intake/acme. Do not rely on chat history; persist state in the repository.
```

Claude Code should read `CLAUDE.md`, which points back to `AGENTS.md` and `agents/client-onboarding.md`.
